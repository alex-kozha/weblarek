# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.
View - слой представления, отвечает за отображение данных на странице.
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:
`baseUrl: string` - базовый адрес сервера
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

#### Данные

Интерфейсы:
`interface IProduct` будет содержать всю информацию о товаре его айди, описание, картинку, название, категорию, цену.
`interface IBuyer` будет содержать информацию о покупателе способо оплаты, емейл, номер телефона, адрес.

### Модели данных

### Класс Busket

Класс предоставляют всю работу с корзиной.

Конструктор класса не принимает параметров, но инцилизирует пустой массив, в котором будет содержаться все товары корзины.

Методы класса:

`getBusket` получает массив товаров, которые находятся в корзине.
`productToBusket` добавляет товар в корзину.
`cleanBusket` очищает всю корзину.
`checkProduct` проверяет находится ли товар в корзине по айди.
`lengthBusket` количество товаров в корзине.
`fullPriceBusket` сумма всех товаров в корзине.
`deleteProduct` удаляет товар из корзины.

### Класс Product

Конструктор класса не принимает параметров, но инцилизирует пустой массив, в котором будет содержаться каталог всех товаров и пустую карточку товара.

Методы класса:

`getCard` получает сохраненную карточку товара.
`getProducts` получает все товары.
`pushProducts` добавляет все товары в массив.
`getProductId` получает продукт по его айди.
`saveCard` сохраняет карточку товара.

### Класс Buyer

Конструктор класса не принимает параметров, но инцилизирует объект с пустыми значениями полей данных пользователя

Методы класса:

`clearBuyerInformation` очищает все данные пользователя
`validation` валидация полей
`buyerInformation` возвращает объект со всеми заполненными данными пользователя
`pushAddress` добавляет адрес покупателя
`pushEmail` добавляет емейл покупателя
`pushPhone` добавляет телефон покупателя
`pushPayment` добавляет значение поля тип оплаты

### Слой коммуникации

### Класс Request

Конструктор принимает и инцилизирует аргумент api который соответсвует интерфейсу IApi.

Методы класса:

`getProducts` будет использовать метод `get` аргумента который был передан в конструктор.
`postOrder` будет использовать метод `post` аргумента который был передан в конструктор.

### Слой представления

### Интерфейсы

`HeaderData` нужен чтобы при инцилизации класса Header передать его в дженрики абстрактного класса Component. Интерфейс содержит поле counter с типом number.

### Класс Header

Классс отвечает за работу с шапкой сайта.

Конструктор класса инцилизирует basketButton c типом HTMLButtonElement, используя функцию ensureElement которая находит елемент в дом дереве, и counterElement, используя функцию ensureElementб которая находит елемент `в дом дереве, c типом HTMLElement. Кнопка basketButton отвечает за нажатие корзины, кнопка counterElement отвечает за счетчик товаров в корзине. А так же мы вешаем на кнопку событие клик на basketButton и при нажатии запускается собыите 'basket:open'

Методы класса:

`counter` принимает число записывает в поле counter.

### Класс Gallery

Gallery наследует от класса Component конструктор принимает контейнер который потом инцилизируется благодаря классу component.

Методы класса:

`catalog` принимает массив HTMLElement и потом просто добавляем его в контейнер

### Классы карточек

Краткое описание карточек:

`Card` - самый главный асбтрактный класс, о которого будет происходить наследование других карточек у него будет два поля цена, карточка.

`CardBusket` - класс карточки которая находиться в корзине, у нее появляться кнопка удаления и индекс.

`ImgCard` - класс, который нужен для создания его производных классов, появляться картинка и категория, наслдеует от Card.

`CatalogCard` - класс производный от imgCard появляется слушатель на карточку

### Класс Card (абстрактный)

Базовый класс для всех карточек. Содержит общие элементы: заголовок и цену.

Поля:

title: string — название товара

price: number | null — цена товара

Методы:

render(data: ICardData): HTMLElement — отрисовывает карточку

### Класс ImgCard (абстрактный)

Наследуется от Card. Добавляет изображение и категорию.

Поля:

category: string — категория товара

image: string — путь к изображению

### Класс CatalogCard

Наследуется от ImgCard. Карточка для каталога товаров.

Конструктор: (container: HTMLElement, actions?: ICardActions)

При клике на карточку вызывает actions.onClick()

### Класс PreviewCard

Наследуется от ImgCard. Карточка для модального окна с деталями товара.

Поля:

currentProduct: IPreviewData | null — текущий товар

Методы:

setProduct(product: IPreviewData) — запоминает товар

setButtonState(inBasket: boolean, available: boolean) — меняет состояние кнопки

render(data: IPreviewData): HTMLElement — отрисовывает карточку

### Класс BuskertCard

Наследуется от Card. Карточка для отображения товара в корзине.

Поля:

index: number — порядковый номер товара

Конструктор: (container: HTMLElement, action?: ICardActions)

При клике на кнопку удаления вызывает action.onClick()

🪟 Модальные окна
Класс Modal
Универсальное модальное окно.

Методы:

open(content: HTMLElement) — открывает модалку с переданным содержимым

close() — закрывает модалку

### Класс BusketModal

Модальное окно корзины.

Поля:

items: HTMLElement[] — массив карточек товаров

total: number — общая сумма

Методы:

clear() — очищает корзину

Событие: basket:order — при клике на кнопку «Оформить»

### Класс Success

Модальное окно с сообщением об успешном заказе.

Поля:

total: number — сумма списания

Событие: success:close — при клике на кнопку закрытия

### Базовый класс Form (абстрактный)

Базовый класс для всех форм.

Поля:

valid: boolean — состояние валидности

errors: string[] — массив ошибок

Методы:

clear() — очищает форму

render(data: IForm): HTMLElement — обновляет состояние формы

### Класс OrderForm

Наследуется от Form. Форма первого шага оформления (оплата и адрес).

Поля:

addressInput: HTMLInputElement — поле адреса

paymentButtons: HTMLButtonElement[] — кнопки выбора оплаты

Методы:

setActivePaymentMethod(method: 'card' | 'cash') — выделяет выбранную кнопку

set address(value: string) — устанавливает значение адреса

### Класс ContactsForm

Наследуется от Form. Форма второго шага (email и телефон).

Поля:

emailInput: HTMLInputElement — поле email

phoneInput: HTMLInputElement — поле телефона

### Презентер

Переменные
currentOrderForm: OrderForm | null — текущий экземпляр формы заказа

currentContactsForm: ContactsForm | null — текущий экземпляр формы контактов

Функции
updateBasketCounter() — обновляет счётчик в шапке

renderCatalog() — отрисовывает каталог товаров

loadProducts() — загружает товары с сервера

### События

card:select — открытие PreviewCard

preview:add-to-basket / remove-from-basket — обновление корзины

basket:open — открытие корзины

basket:order — открытие формы заказа

order:change — валидация и обновление UI

order:submit — открытие формы контактов

contacts:change — валидация и обновление UI

contacts:submit — отправка заказа на сервер

success:close — закрытие модалки
